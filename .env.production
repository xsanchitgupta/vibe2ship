# PUBLIC client-side keys only — these are exposed in the browser bundle anyway,
# so it's safe to commit them. They are inlined into the build (NEXT_PUBLIC_*).
# SECRETS (GEMINI_API_KEY, SUPABASE_SERVICE_ROLE_KEY) are NOT here — set those as
# Cloud Run runtime env vars at deploy time.
NEXT_PUBLIC_SUPABASE_URL=https://dhhxqmdsdmypnzlqeixp.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_QvvMI8xzxX20BFDS-KxcBg_1vqINq-Z
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=AIzaSyCjlTYIXR4CjUeasHeG3L7GndUfwdMVEdM
